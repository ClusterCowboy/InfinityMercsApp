# Refactor Backlog

1. UnitDetails task 2: remove/replace remaining tiny local helpers (Standard `UnitHasVisibleOption`, Cohesive `ParseCostValue`) with shared backend utilities.
2. UnitDetails task 4: unify `MatchesClassificationFilter` and `UnitHasLieutenantOption` into backend filter utilities.
